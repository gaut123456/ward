{
  "name": "ward",
  "version": "0.1.4",
  "description": "Ward : widget de bureau pour lancer une partie classée League of Legends via le LCU.",
  "main": "electron/main.cjs",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --enable-logging",
    "check": "node --check electron/main.cjs && node --check electron/lcu.cjs && node --check electron/roles.cjs && node --check electron/gameflow.cjs && node --check electron/client-ui.cjs && node --check electron/updater.cjs && node --check electron/queues.cjs && node --check electron/shutdown.cjs && node --check electron/champselect.cjs && node --check electron/runes.cjs && node --check electron/preload.cjs && node --check renderer/app.js && node --check renderer/champselect.js && node --check renderer/runes.js && node --check renderer/ready.js",
    "test": "node --test test/*.test.cjs",
    "test:ui": "electron scripts/smoke-ui.cjs",
    "build:icon": "electron scripts/build-icon.cjs",
    "build:exe": "npm run check && npm test && npm run build:icon && electron-builder --win portable --x64 --publish never",
    "test:exe": "node scripts/smoke-exe.cjs",
    "build:site": "node scripts/build-site.cjs"
  },
  "build": {
    "appId": "local.ward",
    "productName": "Ward",
    "directories": { "output": "out" },
    "files": ["electron/**/*", "renderer/**/*", "package.json"],
    "asar": true,
    "win": {
      "icon": "build/icon.ico",
      "target": [{ "target": "portable", "arch": ["x64"] }],
      "signExecutable": false,
      "requestedExecutionLevel": "asInvoker"
    },
    "portable": {
      "artifactName": "Ward-${version}-portable.exe",
      "requestExecutionLevel": "user"
    }
  },
  "devDependencies": {
    "electron": "^37.2.6",
    "electron-builder": "^26.15.3"
  }
}
